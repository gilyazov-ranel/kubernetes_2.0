apiVersion: apps/v1
kind: Deployment
metadata:
  name: cache
  namespace: netology
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cache
  template:
    metadata:
      labels:
        app: cache
    spec:
      containers:
      - name: cache
        image: praqma/network-multitool
        ports:
        - containerPort: 80
        env:
        - name: HTTP_PORT
          value: "80"
        - name: HTTPS_PORT
          value: "443"
---
apiVersion: v1
kind: Service
metadata:
  name: cache-service
  namespace: netology
spec:
  selector:
    app: cache
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP